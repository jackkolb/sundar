// content.js: contains functions used to add general formatting devices to the page content

// contentBreak: adds a line break to the page content
function contentBreak() {
    document.getElementById("content").appendChild(document.createElement("br"));
}